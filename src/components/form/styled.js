import styled from 'styled-components';

export const formWrapper = styled.div`
    input {
        border: 1px solid #ccc;
        border-radius: 8px;
        width: 25%;
        height: 44px;
        padding: 8px;
        font-weight: 500;
    }

    textarea {
        border: 1px solid #ccc;
        border-radius: 8px;
        width: 50%;
        height: 44px;
        padding: 8px;
        font-weight: 500;
    }
` 