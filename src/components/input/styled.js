import styled from 'styled-components';

export const inputWrapper = styled.div`
    input {
        border: 1px solid #ccc;
        border-radius: 8px;
        width: 50%;
        height: 44px;
        padding: 8px;
        font-weight: 500;
    }
` 